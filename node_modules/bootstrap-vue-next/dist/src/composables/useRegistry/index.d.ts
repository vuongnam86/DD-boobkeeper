import { Ref } from 'vue';
import { RegisterShowHideFnInput, RegisterShowHideInstances } from '../../utils/keys';
import { BAppProps } from '../../types/ComponentProps';
export declare const useRegistry: (rtl?: BAppProps["rtl"]) => void;
export declare const _newShowHideRegistry: () => {
    register: ({ id, component, value, toggle, show, hide, registerTrigger, unregisterTrigger, }: RegisterShowHideFnInput) => {
        unregister(): void;
        updateId(newId: string, oldId: string): void;
    };
    values: Ref<Map<string, RegisterShowHideInstances>, Map<string, RegisterShowHideInstances>>;
};
